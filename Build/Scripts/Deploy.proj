<?xml version="1.0" encoding="utf-8"?>
<Project ToolsVersion="4.0" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">

  <PropertyGroup>
    <ImportedDeploy>True</ImportedDeploy>
  </PropertyGroup>

  <Import Project="Deploy.Package.proj" Condition="'$(ImportedDeployPackage)' == ''" />

  <Target Name="Deploy">
    <CallTarget Targets="DeployPackage" Condition="'$(SkipDeployPackage.ToUpper())' != 'TRUE'" />
  </Target>

</Project>